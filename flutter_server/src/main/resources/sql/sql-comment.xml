<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bitc.next502.flutter_server.mapper.CommentMapper">

    <!--  댓글 등록 -->
    <insert id="insertComment">
        INSERT INTO comments (content, user_num, post_id, created_at)
        VALUES (#{content}, #{userNum}, #{postId}, NOW())
    </insert>

    <!--  댓글 조회 -->
    <select id="getComments" resultType="bitc.next502.flutter_server.dto.CommentDTO">
        SELECT comment_id, content, user_num, post_id, created_at, updated_at
        FROM comments
        WHERE post_id = #{postId}
        ORDER BY created_at DESC
    </select>

    <!--  댓글 수정 -->
    <update id="updateComment">
        UPDATE comments
        SET content = #{content},
            updated_at = NOW()
        WHERE comment_id = #{commentId}
          AND user_num = #{userNum}
    </update>

    <!--  댓글 삭제 -->
    <delete id="deleteComment">
        DELETE FROM comments
        WHERE comment_id = #{commentId}
          AND user_num = #{userNum}
    </delete>

</mapper>